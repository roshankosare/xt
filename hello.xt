var x = 10;
var y = 30;
var z = 0;

function add(x , y ){
    var t = x;
    asm(["pop eax", "pop bx", "call print_eax"]);
    t  = y;
    asm(["pop eax", "pop bx", "call print_eax"]);
    return x + y;
}

z = add(x , y );
asm(["pop eax", "pop bx", "call print_eax"]);